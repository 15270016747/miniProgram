var name = 'hwj';
var age = 25;
function getNum(price,num) {
  var num = num || 2
  var f_price = parseFloat(price)
  return f_price.toFixed(num)
}

module.exports = {
  name:name,
  age:age,
  getNum:getNum
}